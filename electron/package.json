{
  "name": "electron",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "dev": "esbuild preload.ts --bundle --external:electron --platform=node --outfile=preload.js --watch",
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "async-call-rpc": "^6.3.1",
    "electron": "^25.0.1"
  },
  "devDependencies": {
    "esbuild": "^0.17.19"
  }
}
